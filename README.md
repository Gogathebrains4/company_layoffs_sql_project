# company_layoffs_sql_project
This project focuses on cleaning and analyzing a global layoffs dataset using MySQL. The goal was to transform raw, inconsistent data into a structured and analysis-ready format, then extract meaningful business insights using SQL.
